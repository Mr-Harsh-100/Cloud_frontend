import { ShowtreeDirective } from './showtree.directive';

describe('ShowtreeDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowtreeDirective();
    expect(directive).toBeTruthy();
  });
});
